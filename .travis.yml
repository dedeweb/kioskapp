#.travis.yml

language: android

node_js:
  - "8.11"

sudo: false

branches:
  only:
  - master

install:
  - npm cache clean
  - npm install
  - echo y | android update sdk -u --filter android-22,android-23

script:
  - npm run build
  
release:
  - exit 0  